# Eigenverft.WebLib.Infrastructure

This folder owns the solution for the existing monolithic infrastructure package and its tests.

```text
./                                                     solution directory
../../prj/Eigenverft.WebLib.Infrastructure/             package project
../../prj/Eigenverft.WebLib.Infrastructure.Tests/       test project
```

Open `Eigenverft.WebLib.Infrastructure.slnx` in Visual Studio or run `dotnet` commands from this directory.
