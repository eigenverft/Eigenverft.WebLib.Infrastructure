# Eigenverft.WebLib.CanonicalHostRedirect

ASP.NET Core middleware for redirecting requests to a configured canonical host with configurable redirect behavior.

Canonical-host redirects preserve the original behavior from Eigenverft.WebLib.Infrastructure.
