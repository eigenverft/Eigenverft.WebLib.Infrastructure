# Eigenverft.WebLib.RequestTrafficShaping

ASP.NET Core request traffic shaping with per-client and server-wide token-bucket rate limits and optional concurrency control.

Request traffic shaping preserves the original public namespaces and behavior from Eigenverft.WebLib.Infrastructure.
